import ChatInput from "./ChatInput"


const ChatBody = (initialChat) => {
  return (
    <div className="w-full h-[calc(100vh-300px)] flex justify-center items-center overflow-y-auto">
      Start a new conversation
    </div>
  )
}

export default ChatBody